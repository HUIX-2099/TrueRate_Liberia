"use client"

/**
 * Sticky bar showing live rate on mobile only. Disabled: renders nothing.
 * Kept so imports do not break (was above header on mobile).
 */
export function StickyMobileRateBar() {
  return null
}
